import { KeepCenteredDirective } from './keep-centered.directive';

describe('KeepCenteredDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepCenteredDirective();
    expect(directive).toBeTruthy();
  });
});
